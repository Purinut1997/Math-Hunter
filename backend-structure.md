# โครงสร้างและระบบการทำงานของโปรเจกต์ (System Architecture)

เอกสารนี้จัดทำขึ้นเพื่อใช้สำหรับอธิบายภาพรวมของระบบ เทคโนโลยีที่ใช้เบื้องหลัง (Under the hood) และวิธีการเชื่อมต่อการทำงานของส่วนต่างๆ ในเกม **Math-Hunter**

## 🛠 เทคโนโลยีหลักที่ใช้งาน (Tech Stack)

โปรเจกต์นี้ถูกพัฒนาขึ้นมาในรูปแบบ Web-based Game (เกมบนเว็บ) โดยใช้เทคโนโลยีและเครื่องมือดังต่อไปนี้:

1. **Phaser (Game Engine)**
   - เป็นหัวใจหลักในการสร้างเกม ใช้สำหรับจัดการกราฟิก (2D), ระบบฟิสิกส์, แอนิเมชัน, ฉากต่างๆ (Scenes) และ Game Loop (การทำงานวนซ้ำเพื่ออัปเดตภาพของเกม)
2. **TypeScript (Programming Language)**
   - ใช้เขียนลอจิกทั้งหมดของเกม การใช้ TypeScript (แทน JavaScript ธรรมดา) ช่วยให้โค้ดมีโครงสร้างที่ชัดเจน เป็นระบบ (Object-Oriented) และสามารถตรวจสอบข้อผิดพลาด (Type-checking) ได้ตั้งแต่ตอนเขียนโค้ด
3. **Vite (Build Tool & Dev Server)**
   - เป็นเครื่องมือยุคใหม่ที่ทำหน้าที่เป็นเซิร์ฟเวอร์จำลองในตอนพัฒนา (รันได้รวดเร็ว) และทำหน้าที่มัดรวมไฟล์ (Bundle) โค้ดทั้งหมดให้พร้อมสำหรับการนำไปปล่อยให้คนเล่นจริง (Production)
4. **HTML5 & CSS3**
   - โครงสร้างพื้นฐานสำหรับแสดงผลหน้าเว็บและจัดระเบียบหน้าจอเกมให้พอดีกับอุปกรณ์ของผู้เล่น

## 🔄 การเชื่อมต่อและการทำงานของระบบ (How It Works)

การทำงานของระบบจะเชื่อมโยงกันเป็นทอดๆ ดังนี้:

### 1. จุดเริ่มต้น (Entry Point)
- เมื่อผู้เล่นเข้าสู่เกม เบราว์เซอร์จะโหลดไฟล์ `index.html` เป็นอันดับแรก
- จากนั้นจะดึงไฟล์ `style.css` มาจัดการหน้าตาของเว็บ และเริ่มโหลดไฟล์สคริปต์หลัก (Vite จะทำหน้าที่ช่วยดึงโค้ด TypeScript มาแปลงให้เบราว์เซอร์อ่านเข้าใจ)

### 2. การสร้างตัวเกม (Game Initialization)
- เมื่อสคริปต์เริ่มทำงาน ไฟล์อย่าง `src/game/Game.ts` จะทำหน้าที่เป็นตัวสตาร์ทเครื่องยนต์
- ระบบจะสร้าง "พื้นที่แสดงผลเกม" (Canvas) ขึ้นมาบนหน้าเว็บผ่าน **Phaser Engine** พร้อมกับโหลดการตั้งค่าพื้นฐานของเกม

### 3. การจัดการฉาก (Scene Management)
- เกมจะถูกแบ่งออกเป็นฉากต่างๆ (Scenes) อย่างชัดเจน เช่น `MainMenuScene` (หน้าเมนูหลัก) 
- แต่ละฉากจะทำงานแยกส่วนกัน เมื่อผู้เล่นกดปุ่มเริ่มเกม ระบบจัดการฉากของ Phaser จะทำการสลับจากฉากเมนู เข้าสู่ฉากการเล่น และเริ่มโหลดทรัพยากร (รูปภาพตัวละคร, เสียง) ที่ต้องใช้ในด่านนั้นๆ

### 4. ระบบเบื้องหลังและการประมวลผล (Game Logic & Data)
- **ฉากการเล่น (Scene)** จะดึงไฟล์ระบบเฉพาะทางมาใช้งาน เช่น `WalkabilityMap.ts` (ใช้ตรวจสอบตารางพิกัดว่าตัวละครเดินไปจุดไหนได้บ้าง ติดกำแพงหรือไม่)
- ระบบถาม-ตอบคณิตศาสตร์ จะมีการดึงชุดคำถาม (เช่นจาก `test-questions.ts`) มาประมวลผล สุ่ม และแสดงผลให้ผู้เล่นตอบ เพื่อโจมตีศัตรูในเกม

## 💡 บทสรุปสำหรับใช้พูดนำเสนอ (Pitch)

> *"เกม **Math-Hunter** ของเราเป็นเกมบนเว็บที่ทำงานแบบ Client-side โดยใช้ **Phaser** เป็นเอนจินหลักในการขับเคลื่อนตัวเกมและจัดการกราฟิก เราใช้ **TypeScript** ในการเขียนโครงสร้างลอจิกทั้งหมดเพื่อให้โค้ดมีระเบียบและปลอดภัยจากบั๊ก และควบคุมการบิลด์โปรเจกต์ด้วย **Vite** 
> 
> การทำงานจะเริ่มจากไฟล์หน้าเว็บ (`index.html`) ที่จะไปกระตุ้นให้ Phaser สร้างหน้าจอเกมขึ้นมา จากนั้นจะแบ่งการทำงานเป็น 'ฉาก' (Scene) ต่างๆ เช่น หน้าเมนู หรือหน้าเล่นเกม ซึ่งแต่ละฉากจะไปเรียกใช้ระบบประมวลผลเบื้องหลัง (เช่น ระบบเดิน, ระบบสุ่มคำถามคณิตศาสตร์) มาทำงานร่วมกันแบบเรียลไทม์ครับ"*
